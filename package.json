{
  "name": "my-netlify-project",
  "version": "1.0.0",
  "description": "Simple HTML deployment to Netlify",
  "main": "index.html",
  "scripts": {
    "start": "browser-sync start --server --no-ghost-mode --files '*.html, *.css'",
    "build": "echo 'No build step required for static HTML'"
  },
  "devDependencies": {
    "browser-sync": "^3.0.0"
  }
}